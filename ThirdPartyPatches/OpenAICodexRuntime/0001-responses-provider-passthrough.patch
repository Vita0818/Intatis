diff --git a/codex-rs/Cargo.lock b/codex-rs/Cargo.lock
index 3db2c3d..92b87f8 100644
--- a/codex-rs/Cargo.lock
+++ b/codex-rs/Cargo.lock
@@ -3564,6 +3564,7 @@ dependencies = [
  "pretty_assertions",
  "schemars 0.8.22",
  "serde",
+ "serde_json",
  "tempfile",
  "toml 0.9.11+spec-1.1.0",
 ]
diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index baaa57c..3cfeb6b 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -94,7 +94,7 @@ use codex_terminal_detection::TerminalName;
 #[derive(Debug, Parser)]
 #[clap(
     author,
-    version,
+    version = "0.145.0-intatis.2",
     // If a sub‑command is given, ignore requirements of the default args.
     subcommand_negates_reqs = true,
     // The executable is sometimes invoked via a platform‑specific name like
diff --git a/codex-rs/codex-api/src/common.rs b/codex-rs/codex-api/src/common.rs
index cafea02..bfd3e0c 100644
--- a/codex-rs/codex-api/src/common.rs
+++ b/codex-rs/codex-api/src/common.rs
@@ -234,6 +234,9 @@ pub struct ResponsesApiRequest {
     pub prompt_cache_key: Option<String>,
     #[serde(skip_serializing_if = "Option::is_none")]
     pub text: Option<TextControls>,
+    /// Intatis-derived 0.145.0 patch: request-owned provider-specific body.
+    #[serde(skip_serializing_if = "Option::is_none")]
+    pub provider: Option<Value>,
     #[serde(skip_serializing_if = "Option::is_none")]
     pub client_metadata: Option<HashMap<String, String>>,
 }
@@ -256,6 +259,7 @@ impl<'a> From<&'a ResponsesApiRequest> for ResponseCreateWsRequest<'a> {
             service_tier: request.service_tier.as_deref(),
             prompt_cache_key: request.prompt_cache_key.as_deref(),
             text: request.text.as_ref(),
+            provider: request.provider.as_ref(),
             generate: None,
             client_metadata: request.client_metadata.clone(),
         }
@@ -287,6 +291,8 @@ pub struct ResponseCreateWsRequest<'a> {
     #[serde(skip_serializing_if = "Option::is_none")]
     pub text: Option<&'a TextControls>,
     #[serde(skip_serializing_if = "Option::is_none")]
+    pub provider: Option<&'a Value>,
+    #[serde(skip_serializing_if = "Option::is_none")]
     pub generate: Option<bool>,
     #[serde(skip_serializing_if = "Option::is_none")]
     pub client_metadata: Option<HashMap<String, String>>,
diff --git a/codex-rs/codex-api/src/endpoint/responses_websocket.rs b/codex-rs/codex-api/src/endpoint/responses_websocket.rs
index 24d6de8..62f12e6 100644
--- a/codex-rs/codex-api/src/endpoint/responses_websocket.rs
+++ b/codex-rs/codex-api/src/endpoint/responses_websocket.rs
@@ -929,6 +929,12 @@ mod tests {
             service_tier: Some("priority".to_string()),
             prompt_cache_key: Some("cache-key".to_string()),
             text: None,
+            provider: Some(json!({
+                "require_parameters": true,
+                "allow_fallbacks": false,
+                "order": ["openai", "azure"],
+                "future_routing": {"mode": "provider-owned"}
+            })),
             client_metadata: Some(HashMap::from([(
                 "traceparent".to_string(),
                 "00-0123456789abcdef0123456789abcdef-0123456789abcdef-01".to_string(),
diff --git a/codex-rs/codex-api/tests/clients.rs b/codex-rs/codex-api/tests/clients.rs
index 105d782..0ff2d7a 100644
--- a/codex-rs/codex-api/tests/clients.rs
+++ b/codex-rs/codex-api/tests/clients.rs
@@ -327,6 +327,7 @@ async fn responses_client_stream_request_preserves_item_ids() -> Result<()> {
         service_tier: None,
         prompt_cache_key: None,
         text: None,
+        provider: None,
         client_metadata: None,
     };
     let expected = serde_json::to_value(&request)?;
@@ -414,6 +415,7 @@ async fn streaming_client_retries_on_transport_error() -> Result<()> {
         service_tier: None,
         prompt_cache_key: None,
         text: None,
+        provider: None,
         client_metadata: None,
     };
     let client = ResponsesClient::new(transport.clone(), provider, Arc::new(NoAuth));
@@ -534,6 +536,7 @@ async fn azure_store_sends_ids_and_headers() -> Result<()> {
         service_tier: None,
         prompt_cache_key: None,
         text: None,
+        provider: None,
         client_metadata: None,
     };
 
diff --git a/codex-rs/config/src/thread_config/remote.rs b/codex-rs/config/src/thread_config/remote.rs
index 7a8e4ce..1ae558f 100644
--- a/codex-rs/config/src/thread_config/remote.rs
+++ b/codex-rs/config/src/thread_config/remote.rs
@@ -183,6 +183,7 @@ fn model_provider_from_proto(
         wire_api,
         query_params: provider.query_params.map(|map| map.values),
         http_headers: provider.http_headers.map(|map| map.values),
+        intatis_responses_provider: None,
         env_http_headers: provider.env_http_headers.map(|map| map.values),
         request_max_retries: provider.request_max_retries,
         stream_max_retries: provider.stream_max_retries,
diff --git a/codex-rs/core/src/client.rs b/codex-rs/core/src/client.rs
index 9b0592d..d8e5de0 100644
--- a/codex-rs/core/src/client.rs
+++ b/codex-rs/core/src/client.rs
@@ -323,6 +323,7 @@ fn responses_request_properties_match(
         service_tier: previous_service_tier,
         prompt_cache_key: previous_prompt_cache_key,
         text: previous_text,
+        provider: previous_provider,
         client_metadata: _,
     } = previous;
     let ResponsesApiRequest {
@@ -340,6 +341,7 @@ fn responses_request_properties_match(
         service_tier: current_service_tier,
         prompt_cache_key: current_prompt_cache_key,
         text: current_text,
+        provider: current_provider,
         client_metadata: _,
     } = current;
 
@@ -357,6 +359,7 @@ fn responses_request_properties_match(
         && previous_service_tier == current_service_tier
         && previous_prompt_cache_key == current_prompt_cache_key
         && previous_text == current_text
+        && previous_provider == current_provider
 }
 
 impl WebsocketSession {
@@ -903,6 +906,7 @@ impl ModelClient {
             service_tier,
             prompt_cache_key,
             text,
+            provider: intatis_responses_provider(self.state.provider.info()),
             client_metadata: Some(responses_metadata.client_metadata()),
         };
         Ok(request)
@@ -1104,6 +1108,10 @@ impl ModelClient {
     }
 }
 
+fn intatis_responses_provider(provider: &ModelProviderInfo) -> Option<serde_json::Value> {
+    provider.intatis_responses_provider.clone()
+}
+
 impl Drop for ModelClientSession {
     fn drop(&mut self) {
         let websocket_session = std::mem::take(&mut self.websocket_session);
diff --git a/codex-rs/core/src/client_common_tests.rs b/codex-rs/core/src/client_common_tests.rs
index b8d0a36..6c484a0 100644
--- a/codex-rs/core/src/client_common_tests.rs
+++ b/codex-rs/core/src/client_common_tests.rs
@@ -124,6 +124,7 @@ fn serializes_text_verbosity_when_set() {
             verbosity: Some(OpenAiVerbosity::Low),
             format: None,
         }),
+        provider: None,
         client_metadata: None,
     };
 
@@ -169,6 +170,7 @@ fn serializes_text_schema_with_strict_format() {
         prompt_cache_key: None,
         service_tier: None,
         text: Some(text_controls),
+        provider: None,
         client_metadata: None,
     };
 
@@ -231,6 +233,7 @@ fn omits_text_when_not_set() {
         prompt_cache_key: None,
         service_tier: None,
         text: None,
+        provider: None,
         client_metadata: None,
     };
 
@@ -255,6 +258,7 @@ fn serializes_flex_service_tier_when_set() {
         prompt_cache_key: None,
         service_tier: Some(ServiceTier::Flex.to_string()),
         text: None,
+        provider: None,
         client_metadata: None,
     };
 
@@ -264,3 +268,34 @@ fn serializes_flex_service_tier_when_set() {
         Some("flex")
     );
 }
+
+#[test]
+fn serializes_intatis_opaque_provider_object_without_field_loss() {
+    let provider = serde_json::json!({
+        "require_parameters": true,
+        "allow_fallbacks": false,
+        "order": ["openai", "azure"],
+        "future_routing": {"mode": "provider-owned"}
+    });
+    let req = ResponsesApiRequest {
+        model: "stealth/ox-alpha".to_string(),
+        instructions: "i".to_string(),
+        input: vec![],
+        tools: Some(vec![]),
+        tool_choice: "auto".to_string(),
+        parallel_tool_calls: true,
+        reasoning: None,
+        store: false,
+        stream: true,
+        stream_options: None,
+        include: vec![],
+        prompt_cache_key: None,
+        service_tier: None,
+        text: None,
+        provider: Some(provider.clone()),
+        client_metadata: None,
+    };
+
+    let value = serde_json::to_value(&req).expect("json");
+    assert_eq!(value["provider"], provider);
+}
diff --git a/codex-rs/core/src/client_tests.rs b/codex-rs/core/src/client_tests.rs
index 3f33aff..8d6f290 100644
--- a/codex-rs/core/src/client_tests.rs
+++ b/codex-rs/core/src/client_tests.rs
@@ -78,6 +78,23 @@ use tracing_subscriber::util::SubscriberInitExt;
 use wiremock::Mock;
 use wiremock::MockServer;
 use wiremock::ResponseTemplate;
+
+#[test]
+fn intatis_provider_config_maps_to_opaque_responses_provider_body() {
+    let mut provider = ModelProviderInfo::default();
+    let provider_body = json!({
+        "require_parameters": true,
+        "allow_fallbacks": false,
+        "order": ["openai", "azure"],
+        "future_routing": {"mode": "provider-owned"}
+    });
+    provider.intatis_responses_provider = Some(provider_body.clone());
+
+    assert_eq!(
+        super::intatis_responses_provider(&provider),
+        Some(provider_body)
+    );
+}
 use wiremock::matchers::method;
 use wiremock::matchers::path;
 
diff --git a/codex-rs/core/src/compact_tests.rs b/codex-rs/core/src/compact_tests.rs
index 35cfd25..2b96f76 100644
--- a/codex-rs/core/src/compact_tests.rs
+++ b/codex-rs/core/src/compact_tests.rs
@@ -270,6 +270,7 @@ fn should_use_remote_compact_task_for_azure_provider() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
diff --git a/codex-rs/core/tests/responses_headers.rs b/codex-rs/core/tests/responses_headers.rs
index 604c0a1..75e7b49 100644
--- a/codex-rs/core/tests/responses_headers.rs
+++ b/codex-rs/core/tests/responses_headers.rs
@@ -81,6 +81,7 @@ async fn responses_stream_includes_subagent_header_on_review() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(0),
         stream_max_retries: Some(0),
@@ -217,6 +218,7 @@ async fn responses_stream_includes_subagent_header_on_other() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(0),
         stream_max_retries: Some(0),
@@ -334,6 +336,7 @@ async fn responses_respects_model_info_overrides_from_config() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(0),
         stream_max_retries: Some(0),
diff --git a/codex-rs/core/tests/suite/client.rs b/codex-rs/core/tests/suite/client.rs
index d78855f..ca60bbc 100644
--- a/codex-rs/core/tests/suite/client.rs
+++ b/codex-rs/core/tests/suite/client.rs
@@ -1452,6 +1452,7 @@ async fn send_provider_auth_request(server: &MockServer, auth: ModelProviderAuth
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(0),
         stream_max_retries: Some(0),
@@ -3187,6 +3188,7 @@ async fn azure_responses_request_includes_store_and_prefixed_item_ids() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(0),
         stream_max_retries: Some(0),
@@ -3845,6 +3847,7 @@ async fn azure_overrides_assign_properties_used_for_responses_url() {
             "Custom-Header".to_string(),
             "Value".to_string(),
         )])),
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
@@ -3934,6 +3937,7 @@ async fn env_var_overrides_loaded_auth() {
             "Custom-Header".to_string(),
             "Value".to_string(),
         )])),
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
diff --git a/codex-rs/core/tests/suite/client_websockets.rs b/codex-rs/core/tests/suite/client_websockets.rs
index d76970a..f8a2bc9 100755
--- a/codex-rs/core/tests/suite/client_websockets.rs
+++ b/codex-rs/core/tests/suite/client_websockets.rs
@@ -2280,6 +2280,7 @@ fn websocket_provider_with_connect_timeout(
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(0),
         stream_max_retries: Some(0),
diff --git a/codex-rs/core/tests/suite/stream_error_allows_next_turn.rs b/codex-rs/core/tests/suite/stream_error_allows_next_turn.rs
index 2c86edc..20d70f6 100644
--- a/codex-rs/core/tests/suite/stream_error_allows_next_turn.rs
+++ b/codex-rs/core/tests/suite/stream_error_allows_next_turn.rs
@@ -74,6 +74,7 @@ async fn continue_after_stream_error() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: Some(1),
         stream_max_retries: Some(1),
diff --git a/codex-rs/core/tests/suite/stream_no_completed.rs b/codex-rs/core/tests/suite/stream_no_completed.rs
index 6e02827..e2d46d8 100644
--- a/codex-rs/core/tests/suite/stream_no_completed.rs
+++ b/codex-rs/core/tests/suite/stream_no_completed.rs
@@ -56,6 +56,7 @@ async fn retries_on_early_close() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         // exercise retry path: first attempt yields incomplete stream, so allow 1 retry
         request_max_retries: Some(0),
diff --git a/codex-rs/model-provider-info/Cargo.toml b/codex-rs/model-provider-info/Cargo.toml
index da018ac..89aacc6 100644
--- a/codex-rs/model-provider-info/Cargo.toml
+++ b/codex-rs/model-provider-info/Cargo.toml
@@ -18,6 +18,7 @@ codex-protocol = { workspace = true }
 http = { workspace = true }
 schemars = { workspace = true }
 serde = { workspace = true, features = ["derive"] }
+serde_json = { workspace = true }
 
 [dev-dependencies]
 codex-utils-absolute-path = { workspace = true }
diff --git a/codex-rs/model-provider-info/src/lib.rs b/codex-rs/model-provider-info/src/lib.rs
index 803d858..a8e71a7 100644
--- a/codex-rs/model-provider-info/src/lib.rs
+++ b/codex-rs/model-provider-info/src/lib.rs
@@ -19,6 +19,7 @@ use http::header::HeaderValue;
 use schemars::JsonSchema;
 use serde::Deserialize;
 use serde::Serialize;
+use serde_json::Value;
 use std::collections::HashMap;
 use std::fmt;
 use std::time::Duration;
@@ -114,6 +115,10 @@ pub struct ModelProviderInfo {
     /// Additional HTTP headers to include in requests to this provider where
     /// the (key, value) pairs are the header name and value.
     pub http_headers: Option<HashMap<String, String>>,
+    /// Intatis-derived 0.145.0 patch: request-owned opaque JSON for the
+    /// Responses top-level `provider` object. Codex transports but does not
+    /// interpret or merge this value with host-owned request fields.
+    pub intatis_responses_provider: Option<Value>,
     /// Optional HTTP headers to include in requests to this provider where the
     /// (key, value) pairs are the header name and _environment variable_ whose
     /// value should be used. If the environment variable is not set, or the
@@ -152,6 +157,13 @@ pub struct ModelProviderAwsAuthInfo {
 
 impl ModelProviderInfo {
     pub fn validate(&self) -> std::result::Result<(), String> {
+        if self
+            .intatis_responses_provider
+            .as_ref()
+            .is_some_and(|value| !value.is_object())
+        {
+            return Err("intatis_responses_provider must be a JSON object".to_string());
+        }
         if self.aws.is_some() {
             if self.supports_websockets {
                 // TODO(celia-oai): Support AWS SigV4 signing for WebSocket
@@ -342,6 +354,7 @@ impl ModelProviderInfo {
                     .into_iter()
                     .collect(),
             ),
+            intatis_responses_provider: None,
             env_http_headers: Some(
                 [
                     (
@@ -386,6 +399,7 @@ impl ModelProviderInfo {
                 AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_HEADER.to_string(),
                 AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_VALUE.to_string(),
             )])),
+            intatis_responses_provider: None,
             env_http_headers: None,
             request_max_retries: None,
             stream_max_retries: None,
@@ -533,6 +547,7 @@ pub fn create_oss_provider_with_base_url(base_url: &str, wire_api: WireApi) -> M
         wire_api,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
diff --git a/codex-rs/model-provider-info/src/model_provider_info_tests.rs b/codex-rs/model-provider-info/src/model_provider_info_tests.rs
index cc7f685..7f4d264 100644
--- a/codex-rs/model-provider-info/src/model_provider_info_tests.rs
+++ b/codex-rs/model-provider-info/src/model_provider_info_tests.rs
@@ -22,6 +22,7 @@ base_url = "http://localhost:11434/v1"
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
@@ -56,6 +57,7 @@ query_params = { api-version = "2025-04-01-preview" }
             "api-version".to_string() => "2025-04-01-preview".to_string(),
         }),
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
@@ -91,6 +93,7 @@ env_http_headers = { "X-Example-Env-Header" = "EXAMPLE_ENV_VAR" }
         http_headers: Some(maplit::hashmap! {
             "X-Example-Header".to_string() => "example-value".to_string(),
         }),
+        intatis_responses_provider: None,
         env_http_headers: Some(maplit::hashmap! {
             "X-Example-Env-Header".to_string() => "EXAMPLE_ENV_VAR".to_string(),
         }),
@@ -157,6 +160,41 @@ fn test_header_auth_uses_chatgpt_codex_base_url() {
     assert_eq!(api_provider.base_url, CHATGPT_CODEX_BASE_URL);
 }
 
+#[test]
+fn intatis_responses_provider_preserves_opaque_object_without_changing_headers() {
+    let provider_body = serde_json::json!({
+        "require_parameters": true,
+        "allow_fallbacks": false,
+        "order": ["openai", "azure"],
+        "future_routing": {"mode": "provider-owned"}
+    });
+    let provider = ModelProviderInfo {
+        http_headers: Some(maplit::hashmap! {
+            "x-visible-test-header".to_string() => "present".to_string(),
+        }),
+        intatis_responses_provider: Some(provider_body.clone()),
+        ..ModelProviderInfo::default()
+    };
+
+    provider
+        .validate()
+        .expect("opaque provider object should validate");
+    let api_provider = provider
+        .to_api_provider(/*auth_mode*/ None)
+        .expect("provider should build");
+    assert_eq!(api_provider.headers["x-visible-test-header"], "present");
+    assert_eq!(provider.intatis_responses_provider, Some(provider_body));
+
+    let invalid = ModelProviderInfo {
+        intatis_responses_provider: Some(serde_json::json!(["not", "an", "object"])),
+        ..ModelProviderInfo::default()
+    };
+    assert_eq!(
+        invalid.validate(),
+        Err("intatis_responses_provider must be a JSON object".to_string())
+    );
+}
+
 #[test]
 fn test_supports_remote_compaction_for_azure_name() {
     let provider = ModelProviderInfo {
@@ -170,6 +208,7 @@ fn test_supports_remote_compaction_for_azure_name() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
@@ -195,6 +234,7 @@ fn test_supports_remote_compaction_for_non_openai_non_azure_provider() {
         wire_api: WireApi::Responses,
         query_params: None,
         http_headers: None,
+        intatis_responses_provider: None,
         env_http_headers: None,
         request_max_retries: None,
         stream_max_retries: None,
@@ -303,6 +343,7 @@ fn test_create_amazon_bedrock_provider() {
                 AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_HEADER.to_string() =>
                     AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_VALUE.to_string(),
             }),
+            intatis_responses_provider: None,
             env_http_headers: None,
             request_max_retries: None,
             stream_max_retries: None,
